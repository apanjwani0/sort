import Foundation

/// SortKit — the headless engine behind `sort`: scan → detect → embed → cluster → index.
/// The GUI app and the `sort` CLI are both thin shells over this library.
public enum SortKit {
    public static let version = "0.1.0-dev"
}
